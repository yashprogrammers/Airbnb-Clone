<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            background: #fff;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        form {
            background: #fff;
            padding: 2rem 2.5rem;
            border-radius: 10px;
            box-shadow: 0 2px 12px rgba(240, 128, 128, 0.15);
            display: flex;
            flex-direction: column;
            gap: 1rem;
            min-width: 300px;
        }
        input {
            padding: 0.7rem;
            border: 1px solid #f08080;
            border-radius: 5px;
            background: #fff;
            font-size: 1rem;
            outline: none;
            transition: border 0.2s;
        }
        input:focus {
            border-color: #f08080;
            box-shadow: 0 0 0 2px #f0808033;
        }
        button {
            background: #f08080;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 0.7rem;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.2s;
        }
        button:hover {
            background: #e06666;
        }
    </style>
</head>
<body>
    <form action="/login" method="post">
        <h1>Login</h1>
        <input type="text" name="username" placeholder="Username">
        <!-- <input type="email" name="email" placeholder="Email"> -->
        <input type="password" name="password" placeholder="Password">
        <button>Login</button>
    </form>
</body>
</html>